{
  "name": "breckr-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard for breckr: authors task specs, shows run history",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "//overrides": "broke-ui depends on react-popper@2, whose peer range stops at React 18, so npm installs a second nested React alongside ours. Two React copies in one tree throw 'Invalid hook call' as soon as a component from the wrong copy uses hooks. react-popper itself works fine on 19 -- its metadata is just stale -- so pin every react to one version.",
  "overrides": {
    "react": "$react",
    "react-dom": "$react-dom"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.4",
    "@tanstack/react-router": "^1.170.18",
    "axios": "^1.19.0",
    "broke-ui": "github:Reyzartz/broke-ui#v0.1.1",
    "lucide-react": "^1.27.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tanstack/react-query-devtools": "^5.101.4",
    "@tanstack/react-router-devtools": "^1.167.0",
    "@tanstack/router-plugin": "^1.168.23",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
